const myNumbers = [1, 2, 3, 4, 5, 6, 7, 8]

// const newNums = myNumbers.map( (value) => { return value} )

// console.log(newNums);

// const newNums = myNumbers
//                 .map( (value) => { return value * 10} )
//                 .map( (value) =>  value + 1 )
//                 .filter( (value) => value > 40 )

// console.log(newNums);
